node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Exported Cloud Run service config — contains secrets in plaintext
service.yaml

# Claude Code local settings (personal machine config — keep launch.json shared)
.claude/settings.local.json
